- name: Prepare the shared ACME directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  loop:
    - /etc/letsencrypt
    - /var/lib/letsencrypt
    - /var/lib/letsencrypt/.well-known
    - /var/lib/letsencrypt/.well-known/acme-challenge
 
