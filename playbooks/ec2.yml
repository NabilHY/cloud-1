- name: EC2 provisioning (infra, firewall, Docker, deploy)
  hosts: all
  become: true
  gather_facts: true
  collections:
    - community.docker
    - community.general
  roles:
    - infra
    - ufw
    - docker_install
    - docker_verify
    - compose
    - ssl
    - smoke_test
    - cleanup
